# Plan Additions

We will use this file to store plan additions as we complete the core objectives required for the working MVP

Create a sandbox for testing enviornments, serialized sessions, deterministic agents to be spawned when needed, deterministic gates (linting, unit, integration tests)